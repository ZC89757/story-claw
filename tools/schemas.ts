/**
 * 各工具输出 JSON 格式规范
 *
 * 统一管理所有 LLM 需要生成的 JSON 格式，以工具名为 key。
 * 每条 schema 会在对应工具的 execute() 返回文本中附加，
 * 让 LLM 知道应该输出什么格式。
 *
 * 规则：所有顶层 JSON 必须有 wrapper key，禁止裸数组。
 */

export const OUTPUT_SCHEMAS: Record<string, string> = {

  // ── 角色 JSON 格式（每角色一个文件，跨集共享） ──────────────────────
  character_json: `
角色 JSON 格式（保存为 {角色名}.json）：
{
  "character": "角色名",
  "age_desc": "年龄外貌描述（如 '18岁少年，身材偏瘦，东亚面孔'）",
  "base_image": "{角色名}_原型.png",
  "current_stage": "当前生效的造型阶段名",
  "appearances": {
    "阶段名": {
      "description": "完整造型一句话描述（上衣+下装+鞋+配饰+随身物品）",
      "image": "{角色名}_{阶段名}.png"
    }
  }
}

阶段名命名规则：用简短的中文描述该阶段特征，如「入学阶段」「受伤后」「获得道具后」。
首次出场时创建初始阶段。当角色外观发生明显变化（受伤、换装、获得道具等）时新增阶段。
严禁：所有字符串字段内不得出现英文双引号 "，如需引用请用「」`,

  // ── 场景 JSON 格式（每地点一个文件，跨集共享） ──────────────────────
  scene_json: `
场景 JSON 格式（保存为 {场景名}.json）：
{
  "location": "场景名",
  "base_image": "{场景名}.png",
  "base_prompt": "底图固定元素描述：建筑结构、家具陈设、空间布局（2-3句，无人物，无光线时间描述）",
  "current_stage": "当前生效的软场景阶段名",
  "soft_scenes": {
    "阶段名": "光线/时间/与情节相关的物件描述（1-2句，不得与 base_prompt 中的固定元素冲突）"
  }
}

阶段名命名规则：用简短的中文描述该时间/状态，如「入学第一天午后」「深夜」「事发后」。
base_prompt 只写固定不变的物理环境，不写光线、时间、临时道具。
soft_scenes 只写变化的部分，补充 base_prompt 没有的内容。
严禁：所有字符串字段内不得出现英文双引号 "，如需引用请用「」`,

  // ── segment_script 输出：按场景切分的剧本 ───────────────────────────
  segment_script: `
切分格式（直接输出 Markdown 文本，不要 JSON 包裹）：

## 场景1：{场景名} · {时间/地点}

{该场景对应的原文段落，原文不改动}

## 场景2：{场景名} · {时间/地点}

{该场景对应的原文段落，原文不改动}

规则：
- 场景名必须与场景 JSON 文件中的 location 完全一致
- 原文逐字保留，不得改写、删减、添加任何内容
- 每个场景之间空一行`,

  // ── direct_storyboard 输出：per-beat 分镜 panels JSON ──────────────
  direct_storyboard: `
输出格式：
{
  "panel_count": 4,
  "panels": [
    { "id": 1, "prompt": "以全景景别拍摄，校门入口处，穿白色T恤背单肩包的男生站在石碑前，面对镜头，一手拖行李箱。背景是高大石碑和铁门门柱。" },
    { "id": 2, "prompt": "以近景景别拍摄，镜头对准穿白色T恤的男生，面对镜头，仰头望向石碑，表情疑惑。背景是铁门门柱，砖石墙体。" }
  ]
}

【prompt 写作要素】
每个 panel 的 prompt 是一段完整的生图提示词，必须包含以下要素：
- 景别：以 远景/全景/近景/特写 景别拍摄
- 人物描述（有角色时）：用位置+衣着特征指代（如「穿白色T恤背单肩包的男生」），允许衣服颜色/款式辅助区分，禁止写年龄、五官等外貌
- 人物朝向（有角色时）：面对镜头/左侧面/右侧面/背对镜头，根据 beat 中角色 facing 和摄像机位置推理
- 动作/表情（有角色时）：简洁描述
- 背景描述（近景/特写时）：根据合成帧图片中的场景环境，推理该角色身后的背景
- 无角色 panel（环境、道具特写、特效）：直接描述画面内容

注意：不要在 prompt 中写画幅比例和摄影风格，这些由代码自动追加。

【景别使用规则】
- 建立镜头 beat（场景首个 beat 或有新角色入画）→ 第 1 张用远景或全景，其余近景或特写
- 非建立镜头 beat → 所有 panel 一律近景或特写，禁止远景和全景

【分镜数量规则】panel_count 只能是 1、4、6 之一：
- 1 张：纯过场、无台词、单一环境或特效镜头
- 4 张：简单过场、单一动作、无台词或少量台词
- 6 张：有多句台词或多个连续动作、多角色交互

【其他约束】
- 禁止使用这些词：侧面光、阴影中、光线偏冷、氛围偏暗、黑暗
- 近景必须明确到具体某个人，不能写「两人」等模糊指代
- 严禁：所有字符串字段内不得出现英文双引号 "，如需引用台词请用「」，否则会破坏 JSON 解析`,

};
