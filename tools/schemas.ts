/** 分场 Agent 的输出格式约束。 */
export const SEGMENT_SCRIPT_SCHEMA = `
切分格式（直接输出 Markdown 文本，不要 JSON 包裹）：

## 场景1：{场景名} · {时间/地点}

{该场景对应的原文段落，原文不改动}

## 场景2：{场景名} · {时间/地点}

{该场景对应的原文段落，原文不改动}

规则：
- 场景名必须与场景 JSON 文件中的 location 完全一致
- 原文逐字保留，不得改写、删减、添加任何内容
- 每个场景之间空一行`;
