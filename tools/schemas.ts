/**
 * 各工具输出 JSON 格式规范
 *
 * 统一管理所有 LLM 需要生成的 JSON 格式，以工具名为 key。
 * 每条 schema 会在对应工具的 execute() 返回文本中附加，
 * 让 LLM 知道应该输出什么格式。
 *
 * 规则：所有顶层 JSON 必须有 wrapper key，禁止裸数组。
 */

export const OUTPUT_SCHEMAS: Record<string, string> = {

  // ── parse_script 输出：scene_data.json ─────────────────────────────
  parse_script: `
输出格式（必须严格遵守，保存为 scene_data.json）：
{
  "episode": "集数或标题（如 '第1集'）",
  "characters": [
    { "name": "角色名", "age_desc": "年龄外貌描述（如 '25岁女性，清秀面容'）", "clothing": "服装描述（越详细越好）" }
  ],
  "locations": ["场景名1", "场景名2"],
注意：locations 只能是人物实际所在的物理空间（如房间、建筑、户外区域），
每个 location 必须能被独立生成为一张无人物的环境背景图。
不得包含景别描述、道具名称、动作或任何非空间性概念。
locations 约束（每条 location 写入前必须满足）：
- 必须是独立的顶层物理空间；若该空间是另一个空间的子区域（如"角落""入口""一侧"等），
  直接使用父空间名称，子区域特征写入该 scene 的 scene_spatial，不得单独列为 location。
- 若该空间与已有底图的某个 location 相同或属于其子区域，直接使用已有 location 名称，不得新建。
  "scenes": [
    {
      "id": "scene_01",
      "scene_type": "normal",
      "location": "场景名（必须与 locations 中的名称一致）",
      "scene_spatial": {
        "left_side": "左侧空间描述（只描述物理实体，禁止写光线、氛围、色调）",
        "right_side": "右侧空间描述（同上）",
        "center_back": "中景/背景描述（同上）",
        "foreground": "前景描述（同上）"
      },
      "beats": [
        {
          "num": 1,
          "title": "镜头标题",
          "characters": ["角色A", "角色B"],
          "emotion": ["角色A情绪/精神状态（一词）", "角色B情绪/精神状态（一词）"],
          "action": ["姿势+肢体动作，如'站立，双手抱胸'（姿势：站立/坐着/蹲下/躺下……，加肢体动作（如有），禁止写表情、眼神等细节信息）", "角色B同上"],
          "facing": ["right", "left"],
          "position": ["窗边第二排课桌旁", "教室门口过道"]
        }
      ]
facing 取值：right | left | front | back，表示角色相对镜头的朝向（right=朝右/left=朝左/front=面对镜头/back=背对镜头）。
与 characters 数组一一对应。根据角色之间的互动关系判断：面对面对话时一个 right 一个 left，独白面对观众时 front。
facing 用途：分镜导演在设计近景/特写时，结合角色朝向和合成帧图片推理该角色身后的背景。

position 取值：描述角色相对场景环境中具体物体/区域的位置（如"窗边第二排课桌旁""沙发左侧""教室门口"）。
与 characters 数组一一对应。必须绑定场景中的具体物体或区域，不能用抽象方位（如"左侧""中间"）。
同一角色在同一场景内未发生位移时，position 描述必须与上一个 beat 完全一致（逐字相同）。
position 用途：画面生成时作为角色站位描述写入合成 prompt，确保跨帧位置一致性。

注意：每个 beat 代表一个独立的视觉空间状态，满足以下任一条件时才创建新 beat：
- 有角色入画（新角色出现在场景中）
- 有角色出画（角色离开场景）
    }
  ]
}

scene_type 取值规则：
- "effects"：所有 beats.characters 均为空，且无实体物理空间（纯黑屏、文字叠层、CG 特效等）
  → location 写视觉效果描述，不得写入顶层 locations 数组
- "environment"：所有 beats.characters 均为空，但有实体物理空间（纯环境镜头、无角色过场）
  → location 必须写入顶层 locations 数组
- "normal"：至少一个 beat 有角色入画
  → location 必须写入顶层 locations 数组

严禁：所有字符串字段内不得出现英文双引号 "，如需引用请用「」，否则会破坏 JSON 解析`,

  // ── direct_storyboard 输出：per-beat 分镜 panels JSON ──────────────
  direct_storyboard: `
输出格式：
{
  "panel_count": 4,
  "panels": [
    { "id": 1, "prompt": "以全景景别拍摄，校门入口处，穿白色T恤背单肩包的男生站在石碑前，面对镜头，一手拖行李箱。背景是高大石碑和铁门门柱。" },
    { "id": 2, "prompt": "以近景景别拍摄，镜头对准穿白色T恤的男生，面对镜头，仰头望向石碑，表情疑惑。背景是铁门门柱，砖石墙体。" }
  ]
}

【prompt 写作要素】
每个 panel 的 prompt 是一段完整的生图提示词，必须包含以下要素：
- 景别：以 远景/全景/近景/特写 景别拍摄
- 人物描述（有角色时）：用位置+衣着特征指代（如「穿白色T恤背单肩包的男生」），允许衣服颜色/款式辅助区分，禁止写年龄、五官等外貌
- 人物朝向（有角色时）：面对镜头/左侧面/右侧面/背对镜头，根据 beat 中角色 facing 和摄像机位置推理
- 动作/表情（有角色时）：简洁描述
- 背景描述（近景/特写时）：根据合成帧图片中的场景环境，推理该角色身后的背景。合成帧只展示了一个机位的画面，近景/特写镜头的背景可能不在画面可见范围内，需要根据场景的整体环境合理想象该方向的背景元素
- 无角色 panel（环境、道具特写、特效）：直接描述画面内容

注意：不要在 prompt 中写画幅比例和摄影风格，这些由代码自动追加。

【景别使用规则】
先判断当前 beat 属于哪种类型：
- 建立镜头 beat：场景中的第一个 beat，或相比前一个 beat 有新角色入画
- 非建立镜头 beat：角色列表和前一个 beat 完全相同
建立镜头 beat → 第 1 张 panel 用远景或全景，其余用近景或特写
非建立镜头 beat → 所有 panel 一律用近景或特写，禁止远景和全景

【分镜数量规则】panel_count 只能是 1、4、6 之一：
- 1 张：纯过场、无台词、单一环境或特效镜头
- 4 张：简单过场、单一动作、无台词或少量台词
- 6 张：有多句台词或多个连续动作、多角色交互

【其他约束】
- 禁止使用这些词：侧面光、阴影中、光线偏冷、氛围偏暗、黑暗
- 近景必须明确到具体某个人，不能写「两人」等模糊指代
- 每帧的所有分镜合起来必须不重不漏地覆盖该 beat 的全部关键内容
- 严禁：所有字符串字段内不得出现英文双引号 "，如需引用台词请用「」，否则会破坏 JSON 解析`,

};
